import type { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = { appId: 'br.com.desafiosolar.app', appName: 'DSB', webDir: 'out', backgroundColor: '#422c70', ios: { contentInset: 'never' } };
export default config;
